import { prisma } from "../../../lib/prisma";
import type { NetworkImportData } from "../types";

export class NetworkRepository {
  async importNetwork(data: NetworkImportData) {
    // We'll implement this next.
  }
}